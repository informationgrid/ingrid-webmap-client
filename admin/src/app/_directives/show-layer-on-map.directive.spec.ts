import { ShowLayerOnMapDirective } from './show-layer-on-map.directive';

describe('ShowLayerOnMapDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowLayerOnMapDirective();
    expect(directive).toBeTruthy();
  });
});
