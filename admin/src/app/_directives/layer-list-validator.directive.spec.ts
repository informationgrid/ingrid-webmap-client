import { LayerListValidatorDirective } from './layer-list-validator.directive';

describe('LayerListValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new LayerListValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
