import { ShowCapabilitiesDirective } from './show-capabilities.directive';

describe('ShowCapabilitiesDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowCapabilitiesDirective();
    expect(directive).toBeTruthy();
  });
});
