import { ShowURLDirective } from './show-url.directive';

describe('ShowURLDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowURLDirective();
    expect(directive).toBeTruthy();
  });
});
