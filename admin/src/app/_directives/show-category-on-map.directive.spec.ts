import { ShowCategoryOnMapDirective } from './show-category-on-map.directive';

describe('ShowCategoryOnMapDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowCategoryOnMapDirective();
    expect(directive).toBeTruthy();
  });
});
