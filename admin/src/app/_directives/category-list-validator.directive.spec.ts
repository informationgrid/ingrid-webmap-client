import { CategoryListValidatorDirective } from './category-list-validator.directive';

describe('CategoryListValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CategoryListValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
